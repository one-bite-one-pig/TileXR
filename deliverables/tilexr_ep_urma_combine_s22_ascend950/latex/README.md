# LaTeX Report

Build with a TeX Live distribution that provides `ctex`:

```bash
latexmk -xelatex ascend950_tilexr_combine_practice.tex
```

The source is UTF-8 and targets XeLaTeX. The delivery machine did not have a
TeX engine installed, so the `.tex` source is included but no generated PDF is
committed.
